export const examples: Array<{
  id: string;
  label: string;
  markdown: string;
}> = [
  {
    id: "strategy",
    label: "产品说明",
    markdown: `# 把一次 AI API 调用讲清楚：二进制的耳语的编辑与发布工作流

公众号：二进制的耳语，作者：青玉白露

一次 AI API 调用的价值，不只在于拿到一段返回文本，更在于让编辑者能回看输入如何变成初稿，再变成可发布的文章。本文以一条调用链为线索，整理二进制的耳语的编辑与发布工作流。

## 先写清编辑意图，再发起调用

调用前，我们先写一段简短的编辑 brief：面向谁、要解释什么、哪些材料是事实来源、哪些说法仍需人工确认。这个步骤会多花一点时间，却能避免把模糊需求交给模型后反复返工。

**模型负责提供候选表达，人负责守住事实边界。** *阅读体验和准确性需要同时被照顾。* ~~把所有历史对话无差别粘贴进去~~ 往往只会模糊本次任务的重点。

### 请求中需要交代的内容

- 系统约束：说明语气、范围和不应编造的事实。
- 用户任务：提供背景材料，以及希望读者获得的答案。
  - [x] 明确文章主题与目标读者
  - [x] 列出必须出现的术语与参考链接
  - [ ] 对引用和截图做人工复核
- 输出约束：要求使用小标题、短段落和可复制的代码片段。

> 一次调用不是文章的终点。它只是把编辑判断转化为初稿的开始，之后仍需要审阅、排版和发布前检查。

## 把 API 返回当作待编辑稿

我们用结构化输入保存上下文，再把模型返回的文本视为待编辑稿，而不是直接可发布稿。关于平台能力与规范，可查阅 [微信开放文档](https://developers.weixin.qq.com/)，并在正文中说明读者为什么需要打开它。

生成后的初稿会进入一个简单但明确的顺序：

1. 编辑先核对标题是否回答了读者的问题。
2. 再检查事实、链接和代码是否仍与来源一致。
3. 最后把确认过的内容交给 Markdown 渲染与公众号排版。

---

### 一段足够小的 TypeScript 调用示例

\`\`\`ts
type DraftRequest = {
  topic: string;
  sourceNotes: string[];
};

export async function createDraft(input: DraftRequest) {
  return callAiApi({
    prompt: input.topic,
    context: input.sourceNotes,
    temperature: 0.4
  });
}
\`\`\`

代码块只展示调用形状，不假设某个具体供应商的生产配置。生成结果仍需要由编辑确认事实、语气和引用是否合适。

![编辑工作流中的代码与笔记](https://images.unsplash.com/photo-1516321318423-f06f85e504b3?auto=format&fit=crop&w=1200&q=80)

图片可以帮助读者在长文中切换注意力，但它不替代说明文字。这里的配图对应从材料、提示词到编辑稿的整理过程，实际发布时仍应补充来源和版权信息。

## 发布前，让 Markdown 保留结构

二进制的耳语把 Markdown 作为中间交付物保留下来，是为了让标题、引用、列表、代码和图片都有可追溯的文本来源。主题切换只改变展示层，文章结构不应随之丢失。

### 最后一轮检查清单

- [x] H1、H2 与 H3 的层级可以独立阅读。
- [x] **重点结论**、*补充说明* 与 ~~不再适用的写法~~ 有明确的视觉区分。
- [x] 行内参数如 \`temperature\` 与代码块都能保持可读。
- [ ] 在公众号后台预览外链和图片加载情况。

当稿件通过检查后，再由编辑在后台确认封面、摘要和发布时间。这个流程没有承诺某个固定产出或效果，它只是在每一步保留了可以回看、可以修订的依据。
`
  },
  {
    id: "ops",
    label: "运营周报",
    markdown: `# 运营周报

本周重点围绕内容生产效率展开，结论非常明确：先统一格式，再谈自动发布。

## 数据摘要

1. 新增主题样式：2 套
2. 已验证的 Markdown 文章：6 篇
3. 需要在第二阶段处理的能力：图片上传、草稿发布

> 同一篇内容如果在预览站和发布端输出不一致，后面的所有自动化都没有意义。

### 风险提示

- 表格暂不做高保真
- 图片仍使用外链占位
- 公众号后台兼容性还需要真机验证
`
  },
  {
    id: "deep-dive",
    label: "技术长文",
    markdown: `# 从 Markdown 到公众号 HTML，这一层抽象为什么不能省

很多团队一开始都会觉得：先做个正则替换脚本，把标题、段落、引用拼起来，不就够了吗？

但实际走到第二套主题，问题就会集中爆发：

- Web 预览和实际导出不是一套逻辑
- 一改样式就要改渲染代码
- 列表、引用、代码块的表现开始互相影响

## 什么时候必须引入 Core

当你要同时满足下面三件事时，就不能再把逻辑散落在页面里：

1. 支持多主题切换
2. 需要 Skill 批量调用
3. 后续还想接草稿发布能力

> 最危险的方案不是“做得慢”，而是“先做快版本，后面再推倒重来”。

### 兼容性要点

1. 输出以内联样式为主
2. 结构尽量保守，避免过深嵌套
3. 图片先保留占位 URL，上传适配放在 Core 之外

### 真实内容通常还会包含

- 多段长文本
- [外链引用](https://developers.weixin.qq.com/)
- \`inline code\`
- 多个二三级标题交替出现

---

\`\`\`ts
type RenderResult = {
  html: string;
  meta: {
    title?: string;
    excerpt?: string;
  };
};
\`\`\`

![系统结构示意](https://images.unsplash.com/photo-1518770660439-4636190af475?auto=format&fit=crop&w=1200&q=80)
`
  }
];
